# Stars Cash Flow reseller API key — get one from @StarsCashFlowbot → Reseller.
# Tied to a USD balance; `add` spends it. Never commit the real value.
STARS_CASH_FLOW_API_KEY=

# API endpoint (default shown).
STARS_CASH_FLOW_API_BASE=https://api-stars.ros.media/api/v2
